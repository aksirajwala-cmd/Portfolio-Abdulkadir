# This project currently uses a public form-service endpoint in src/config.js.
# If you switch to a provider that needs a public client ID, place only public values here.
# Never put SMTP passwords, private API keys, or secrets in frontend code.
